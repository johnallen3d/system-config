[task_config]
includes = [".mise/tasks"]

[tasks.nix-rebuild]
description = "Apply darwin-rebuild (updates flake by default); logs to temp file"
file = "./.mise/tasks/nix-rebuild"

[tasks.pi-refresh]
description = "Refresh nix-managed pi packages for personal + work profiles"
file = "./.mise/tasks/pi-refresh"
