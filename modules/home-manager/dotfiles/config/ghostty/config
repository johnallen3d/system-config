# switch to the prerelease channel (or tip)
auto-update-channel = stable

# appearance
bold-is-bright = true
macos-titlebar-style = hidden
macos-icon = custom
macos-custom-icon = /Users/john.allen/.config/ghostty/tokyo-night-moon.icns
theme = tokyo-night-moon
window-colorspace = display-p3
window-padding-x = 4

# shader
custom-shader = ./cursor-shader.glsl

# font-family = "JetBrainsMono Nerd Font Mono" # this is built-in default
font-size = 15.5
font-thicken = true

# keymaps
keybind = clear

keybind = global:cmd+`=toggle_quick_terminal

keybind = super+shift+r=reload_config
keybind = super+shift+i=inspector:toggle

keybind = super+c=copy_to_clipboard
keybind = super+f=start_search
# don't use escape for `end_search` as it conflicts with nvim 🤦‍♂️
keybind = super+shift+f=end_search
# keybind = super+shift+f=write_scrollback_file:paste
keybind = super+q=close_surface
keybind = super+v=paste_from_clipboard

keybind = super+equal=increase_font_size:1
keybind = super+minus=decrease_font_size:1
keybind = super+zero=reset_font_size

# Herdr owns workspace, tab, and pane shortcuts. Since defaults are cleared
# above, leaving these chords unbound passes them through without redundant
# unbind entries.
# Herdr has no equivalent for equalizing panes, so Ghostty keeps this one.
keybind = super+ctrl+equal=equalize_splits

# for claude code 🙄
keybind = shift+enter=text:\n

# other features
confirm-close-surface = false
copy-on-select = clipboard
quit-after-last-window-closed = true
# allow cursor to be a block: https://ghostty.org/docs/config/reference#cursor-style
shell-integration-features = no-cursor
clipboard-paste-protection = false
